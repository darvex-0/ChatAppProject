rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read (download) and write (upload) operations
      // ONLY if the user is authenticated (logged in).
      allow read, write: if request.auth != null;
    }
  }
}
